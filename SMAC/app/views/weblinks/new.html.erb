<%= form_with(model: [ @band, @band.weblinks.build ], local: true) do |form| %>

    <% if @weblink.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@weblink.errors.count, "error") %> prohibited this weblink from being saved:
      </h2>
    <ul>
      <% @weblink.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
    </div>
  <% end %>

  <p>
    <%= form.label :title %> <br>
    <%= form.text_field :title %>
  <p>

  <p>
    <%= form.label :url %> <br>
    <%= form.text_field :url %>
  </p>

  <p>
    <%= form.submit %>
  </p>

<% end %>

<hr>

<%= link_to "Back", bands_path %>
